apiVersion: v1
kind: Service
metadata:
  name: zabbix-web
  namespace: zabbix2
  labels:
    app: zabbix-web
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30000
  selector:
    app: zabbix-web
